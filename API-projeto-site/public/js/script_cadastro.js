/* function analisar() {
    var aux_email = inp_email.value;
    var aux_telefone = inp_telefone.value;
    var aux_cnpj = inp_cnpj.value;
    var aux_numero = inp_numero.value;


    if (inp_nome.value == '' || inp_cnpj.value == '' || inp_email.value == '' || inp_telefone.value == '' || inp_cidade.value == '' || inp_rua.value == '' || inp_numero.value == '') {
        alert('Preencha todos os campos');
        msg_aviso.innerHTML = 'Preencha todos os campos!';

    } else if (aux_email.indexOf('@') < 0 && aux_email.indexOf('.') < 0) {
        alert('Escreva um email válido');
        msg_aviso.innerHTML = 'Escreva um email válido';

    } else if ((aux_numero >= 0) && (aux_telefone >= 0) && (aux_cnpj >= 0)) {
        window.location.href = "AreaDoCliente.html";

    } else {
        alert('Alguns campos só aceitam números. Verifique novamente');
        msg_aviso.innerHTML = 'Alguns campos só aceitam números. Verifique!';
    }

} */